--- gptme/init.py
+++ gptme/init.py
@@ -119,6 +119,11 @@
     # set httpx logging to WARNING
     logging.getLogger("httpx").setLevel(logging.WARNING)
     logging.getLogger("httpcore").setLevel(logging.WARNING)
+    # silence MCP client chatter — INFO-level messages dump full tool
+    # specs at session start (one ~3KB block per server) which dwarfs
+    # the actual conversation. Keep WARNING and above.
+    logging.getLogger("gptme.mcp").setLevel(logging.WARNING)
+    logging.getLogger("mcp").setLevel(logging.WARNING)
 
     # Register cleanup handler
 
