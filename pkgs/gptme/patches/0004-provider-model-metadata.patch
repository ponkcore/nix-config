--- gptme/config/models.py	2026-06-11 03:13:14.862617173 +0300
+++ gptme/config/models.py	2026-06-11 03:13:15.377603886 +0300
@@ -6,6 +6,10 @@
     api_key: str | None = None
     api_key_env: str | None = None
     default_model: str | None = None
+    models: dict = field(default_factory=dict)
+    # Per-model metadata (context, max_output, supports_vision,
+    # supports_reasoning). Keys are model IDs relative to
+    # the provider (the part after <provider>/).
 
     def get_api_key(self, config: "Config") -> str:
         """Get the API key from direct value or environment variable."""
