--- gptme/tools/base.py
+++ gptme/tools/base.py
@@ -695,7 +695,9 @@
                 json_parameters: dict[str, str] = {}
                 for index, param in enumerate(tool.parameters):
-                    json_parameters[param.name] = args[index]
+                    if index < len(args):
+                        json_parameters[param.name] = args[index]
+                    # else: skip params without matching args (MCP examples)
 
                 return json_parameters
         return {}
